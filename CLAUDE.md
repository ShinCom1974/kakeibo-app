# kakeibo-app

家計簿アプリ。収入・支出の記録・管理・可視化を目的とした Webアプリケーション。

## プロジェクト概要

- **目的**: 日々の収支を記録し、月次・年次の家計状況を把握する
- **ディレクトリ**: `c:\shinji_work\SAMURAI\claude\kakeibo-app`

## Git 運用ルール

**コードを変更するたびに必ず GitHub へプッシュすること。**

具体的な手順:

1. 変更をステージング: `git add <変更ファイル>`
2. コミット: `git commit -m "<わかりやすいメッセージ>"`
3. プッシュ: `git push origin <ブランチ名>`

### コミットメッセージの書き方

- 日本語・英語どちらでも可
- 変更内容が一目でわかるように書く
- 例: `feat: 収支入力フォームを追加`、`fix: 月次集計の計算バグを修正`

### ブランチ運用

- `main`: 本番相当の安定ブランチ
- 機能追加は `feature/<機能名>` ブランチで作業し、完了後に `main` へマージ
- バグ修正は `fix/<内容>` ブランチで作業

### 注意事項

- `.env` や秘密情報を含むファイルは絶対にコミットしない
- `git push --force` は原則禁止（明示的な指示がある場合のみ許可）
- プッシュ前に `git status` と `git diff` で変更内容を確認する

## 開発環境

（プロジェクト初期化後に記載）

## ディレクトリ構成

（プロジェクト初期化後に記載）

## コマンド

（プロジェクト初期化後に記載）
